# my-cooking-game

Repository for the Baking Prototype and cooking game prototype.

This repository will receive a `baking-prototype` branch with a Unity project scaffold containing scripts, a sample scene, prefabs, and recipe JSON for a web-first baking prototype (mix → proof → bake).

Instructions for the owner (iam3c):
- After cloning, open the project in Unity 2022.3 LTS (recommended).
- The `baking-prototype` branch will be created by the Copilot assistant and contain the prototype files.
